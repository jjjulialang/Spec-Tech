# Dependencies and build output
node_modules/
.wrangler/
dist/

# Secrets — never commit
.dev.vars
.env

# Private datasets — these must NOT reach the public repo.
# `wrangler deploy` still uploads them because wrangler does not read .gitignore.
assets/datasets/test/
assets/datasets/validation/

# Local scratch
*.tar.gz
.DS_Store
output.json
__pycache__/
.venv/

# Provisioned event keys — never commit
team-keys.csv
